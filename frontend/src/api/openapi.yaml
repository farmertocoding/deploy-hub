openapi: 3.0.3
info:
  title: Deploy Hub API
  version: 0.0.1
paths:
  /api/auth/login/:
    post:
      operationId: auth_login_create
      description: |-
        CSRF-protected even though unauthenticated (round-1 finding: login-CSRF —
        a cross-site page must not be able to log the victim into an attacker account).
        The SPA fetches /api/auth/me/ on load, which plants the CSRF cookie.
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Login'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Login'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Login'
        required: true
      security:
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                additionalProperties: {}
          description: ''
  /api/auth/logout/:
    post:
      operationId: auth_logout_create
      tags:
      - auth
      security:
      - cookieAuth: []
      responses:
        '200':
          description: No response body
  /api/auth/me/:
    get:
      operationId: auth_me_retrieve
      description: |-
        Session hydration for the SPA. AllowAny: the anonymous response is the
        SPA's pre-login bootstrap and plants the CSRF cookie (get_token) so the
        login POST itself can be CSRF-checked.
      tags:
      - auth
      security:
      - cookieAuth: []
      - {}
      responses:
        '200':
          description: No response body
  /api/auth/totp/confirm/:
    post:
      operationId: auth_totp_confirm_create
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Confirm'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Confirm'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Confirm'
        required: true
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                additionalProperties: {}
          description: ''
  /api/auth/totp/enroll/:
    post:
      operationId: auth_totp_enroll_create
      tags:
      - auth
      security:
      - cookieAuth: []
      responses:
        '200':
          description: No response body
  /api/demo-jobs/:
    post:
      operationId: demo_jobs_create
      tags:
      - demo-jobs
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DemoJob'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DemoJob'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DemoJob'
        required: true
      security:
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                type: object
                additionalProperties: {}
          description: ''
  /api/topics/{topic}/snapshot/:
    get:
      operationId: topics_snapshot_retrieve
      description: 'Snapshot-then-stream (§D7): every snapshot returns {seq, data}
        from the same counter.'
      parameters:
      - in: path
        name: topic
        schema:
          type: string
        required: true
      tags:
      - topics
      security:
      - cookieAuth: []
      responses:
        '200':
          description: No response body
  /api/v1/cloudflare/connect/:
    post:
      operationId: v1_cloudflare_connect_create
      tags:
      - v1
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CloudflareConnect'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CloudflareConnect'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CloudflareConnect'
        required: true
      security:
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CloudflareConnectResult'
          description: ''
  /api/v1/findings/:
    get:
      operationId: v1_findings_retrieve
      parameters:
      - in: query
        name: entity
        schema:
          type: string
          maxLength: 128
          minLength: 1
      - in: query
        name: severity
        schema:
          enum:
          - p1
          - p2
          - p3
          type: string
          minLength: 1
        description: |-
          * `p1` - P1
          * `p2` - P2
          * `p3` - P3
      - in: query
        name: state
        schema:
          enum:
          - open
          - acked
          - resolved
          - accepted
          type: string
          minLength: 1
        description: |-
          * `open` - Open
          * `acked` - Acked
          * `resolved` - Resolved
          * `accepted` - Accepted
      tags:
      - v1
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FindingListSnapshot'
          description: ''
  /api/v1/findings/{id}/:
    get:
      operationId: v1_findings_retrieve_2
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      tags:
      - v1
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FindingDetailSnapshot'
          description: ''
  /api/v1/findings/{id}/transition/:
    post:
      operationId: v1_findings_transition_create
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      tags:
      - v1
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Transition'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Transition'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Transition'
        required: true
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FindingDetailSnapshot'
          description: ''
  /api/v1/map/:
    get:
      operationId: v1_map_retrieve
      description: Table-backed map.graph snapshot (§D7 / MAP-96-GRAPH-V1).
      tags:
      - v1
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MapSnapshot'
          description: ''
  /api/v1/projects/:
    get:
      operationId: v1_projects_list
      description: |-
        What the readiness screen renders its left column from (F7-lite).

        tier COUNTS here, full check bodies from /readiness/ — the list stays cheap when
        projects grow. manifest_current answers the one freshness question the data model
        can answer honestly: does the latest manifest correspond to the CURRENT scan?
        (A true warnings-diff-since-last-manifest would need the prior report stored,
        which it isn't — noted in the F7 design decision rather than faked.)
      tags:
      - v1
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProjectSummary'
          description: ''
  /api/v1/projects/{project_id}/readiness/:
    get:
      operationId: v1_projects_readiness_retrieve
      description: |-
        The three-tier readiness report (§5.3), tiered server-side.

        Tiering happens here rather than in the client so the CLI, the UI and the pipeline
        all agree on what counts as a blocker.
      parameters:
      - in: path
        name: project_id
        schema:
          type: integer
        required: true
      tags:
      - v1
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Readiness'
          description: ''
  /api/v1/sites/{site_id}/:
    patch:
      operationId: v1_sites_partial_update
      description: Record the operator's Caddy-ownership decision. Never writes dns_zone.
      parameters:
      - in: path
        name: site_id
        schema:
          type: integer
        required: true
      tags:
      - v1
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedSiteEdgeOwner'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedSiteEdgeOwner'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedSiteEdgeOwner'
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SiteEdgeOwner'
          description: ''
  /api/v1/sites/{site_id}/env/:
    get:
      operationId: v1_sites_env_retrieve
      description: GET names; PUT replaces / PATCH merges values; POST applies the
        current env.
      parameters:
      - in: path
        name: site_id
        schema:
          type: integer
        required: true
      tags:
      - v1
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnvNames'
          description: ''
    post:
      operationId: v1_sites_env_create
      description: GET names; PUT replaces / PATCH merges values; POST applies the
        current env.
      parameters:
      - in: path
        name: site_id
        schema:
          type: integer
        required: true
      tags:
      - v1
      security:
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnvApply'
          description: ''
    put:
      operationId: v1_sites_env_update
      description: GET names; PUT replaces / PATCH merges values; POST applies the
        current env.
      parameters:
      - in: path
        name: site_id
        schema:
          type: integer
        required: true
      tags:
      - v1
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnvWrite'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EnvWrite'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EnvWrite'
        required: true
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnvNames'
          description: ''
    patch:
      operationId: v1_sites_env_partial_update
      description: GET names; PUT replaces / PATCH merges values; POST applies the
        current env.
      parameters:
      - in: path
        name: site_id
        schema:
          type: integer
        required: true
      tags:
      - v1
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedEnvWrite'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedEnvWrite'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedEnvWrite'
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnvNames'
          description: ''
  /api/v1/sites/{site_id}/manifest/:
    get:
      operationId: v1_sites_manifest_retrieve
      description: POST materializes version N+1; GET returns the latest frozen manifest.
      parameters:
      - in: path
        name: site_id
        schema:
          type: integer
        required: true
      tags:
      - v1
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
          description: ''
    post:
      operationId: v1_sites_manifest_create
      description: POST materializes version N+1; GET returns the latest frozen manifest.
      parameters:
      - in: path
        name: site_id
        schema:
          type: integer
        required: true
      tags:
      - v1
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Materialize'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Materialize'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Materialize'
      security:
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Manifest'
          description: ''
  /api/v1/sites/{site_id}/rollback/:
    post:
      operationId: v1_sites_rollback_create
      description: 'POST: roll the site back to its latest succeeded Deployment.'
      parameters:
      - in: path
        name: site_id
        schema:
          type: integer
        required: true
      tags:
      - v1
      security:
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RollbackResult'
          description: ''
  /api/v1/sites/{site_id}/wizard/:
    get:
      operationId: v1_sites_wizard_retrieve
      description: GET the question set and current answers; PATCH a partial answer
        set.
      parameters:
      - in: path
        name: site_id
        schema:
          type: integer
        required: true
      tags:
      - v1
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WizardState'
          description: ''
    patch:
      operationId: v1_sites_wizard_partial_update
      description: GET the question set and current answers; PATCH a partial answer
        set.
      parameters:
      - in: path
        name: site_id
        schema:
          type: integer
        required: true
      tags:
      - v1
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAnswers'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAnswers'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAnswers'
      security:
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WizardState'
          description: ''
components:
  schemas:
    ActionEnum:
      enum:
      - ack
      - resolve
      - accept_risk
      type: string
      description: |-
        * `ack` - ack
        * `resolve` - resolve
        * `accept_risk` - accept_risk
    CertRefusal:
      type: object
      properties:
        detail:
          type: string
        finding_id:
          type: integer
      required:
      - detail
      - finding_id
    CloudflareConnect:
      type: object
      properties:
        token:
          type: string
          writeOnly: true
          minLength: 1
      required:
      - token
    CloudflareConnectResult:
      type: object
      properties:
        account:
          $ref: '#/components/schemas/DnsAccountConnected'
        zone:
          $ref: '#/components/schemas/DnsZoneConnected'
      required:
      - account
      - zone
    Confirm:
      type: object
      properties:
        otp_code:
          type: string
          pattern: ^\d{6}$
      required:
      - otp_code
    DemoJob:
      type: object
      properties:
        name:
          type: string
          pattern: ^[a-z][a-z0-9-]{1,30}$
        delay:
          type: number
          format: double
          maximum: 5.0
          minimum: 0.05
          default: 0.5
        confirm_warnings:
          type: boolean
          default: false
      required:
      - name
    DnsAccountConnected:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        provider:
          $ref: '#/components/schemas/ProviderEnum'
        label:
          type: string
          maxLength: 128
      required:
      - id
      - label
    DnsZoneConnected:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        name:
          type: string
          maxLength: 253
        provider_zone_id:
          type: string
          maxLength: 64
        purpose:
          $ref: '#/components/schemas/PurposeEnum'
      required:
      - id
      - name
    EdgeOwnerEnum:
      enum:
      - host_caddy
      - site_caddy
      type: string
      description: |-
        * `host_caddy` - Host Caddy
        * `site_caddy` - Site Caddy
    EnvApply:
      type: object
      properties:
        deployment_id:
          type: integer
      required:
      - deployment_id
    EnvNames:
      type: object
      properties:
        names:
          type: array
          items:
            type: string
        config_stale:
          type: boolean
      required:
      - config_stale
      - names
    EnvWrite:
      type: object
      properties:
        env:
          type: object
          additionalProperties:
            type: string
          writeOnly: true
          description: name -> value. Values are write-only and stored in the vault.
      required:
      - env
    Finding:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        source_engine:
          type: string
          maxLength: 64
        severity:
          $ref: '#/components/schemas/SeverityEnum'
        entity:
          type: string
          maxLength: 128
        title:
          type: string
          maxLength: 256
        body:
          type: string
        fix_action:
          type: string
          maxLength: 256
        state:
          $ref: '#/components/schemas/StateEnum'
        first_seen:
          type: string
          format: date-time
        last_seen:
          type: string
          format: date-time
        fingerprint:
          type: string
          maxLength: 128
        accepted_reason:
          type: string
          maxLength: 256
      required:
      - entity
      - fingerprint
      - id
      - severity
      - source_engine
      - title
    FindingDetailSnapshot:
      type: object
      properties:
        seq:
          type: integer
        data:
          $ref: '#/components/schemas/Finding'
      required:
      - data
      - seq
    FindingListSnapshot:
      type: object
      properties:
        seq:
          type: integer
        data:
          type: array
          items:
            $ref: '#/components/schemas/Finding'
      required:
      - data
      - seq
    KindEnum:
      enum:
      - zone
      - host
      - container
      - hub
      - edge
      type: string
      description: |-
        * `zone` - zone
        * `host` - host
        * `container` - container
        * `hub` - hub
        * `edge` - edge
    Login:
      type: object
      properties:
        username:
          type: string
        password:
          type: string
        otp_code:
          type: string
      required:
      - password
      - username
    Manifest:
      type: object
      properties:
        version:
          type: integer
        schema_version:
          type: integer
        body: {}
        scan_report_hash:
          type: string
        created_at:
          type: string
          format: date-time
      required:
      - body
      - created_at
      - scan_report_hash
      - schema_version
      - version
    MapEdge:
      type: object
      properties:
        a:
          type: string
        b:
          type: string
        path:
          $ref: '#/components/schemas/PathEnum'
      required:
      - a
      - b
      - path
    MapGraph:
      type: object
      properties:
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/MapNode'
        edges:
          type: array
          items:
            $ref: '#/components/schemas/MapEdge'
      required:
      - edges
      - nodes
    MapNode:
      type: object
      properties:
        id:
          type: string
        kind:
          $ref: '#/components/schemas/KindEnum'
        label:
          type: string
        status:
          type: string
        parent:
          type: string
      required:
      - id
      - kind
      - label
      - status
    MapSnapshot:
      type: object
      properties:
        seq:
          type: integer
        data:
          $ref: '#/components/schemas/MapGraph'
      required:
      - data
      - seq
    Materialize:
      type: object
      properties:
        confirm_warnings:
          type: boolean
          default: false
    PatchedAnswers:
      type: object
      properties:
        answers:
          type: object
          additionalProperties: {}
          description: question id -> answer. Partial sets are fine; the wizard saves
            as you go.
      required:
      - answers
    PatchedEnvWrite:
      type: object
      properties:
        env:
          type: object
          additionalProperties:
            type: string
          writeOnly: true
          description: name -> value. Values are write-only and stored in the vault.
    PatchedSiteEdgeOwner:
      type: object
      description: PATCH /api/v1/sites/{id}/ — {edge_owner} only (design note §7 I-edge).
      properties:
        edge_owner:
          $ref: '#/components/schemas/EdgeOwnerEnum'
    PathEnum:
      enum:
      - public
      - mesh
      type: string
      description: |-
        * `public` - public
        * `mesh` - mesh
    ProjectSummary:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        scanned_at:
          type: string
          format: date-time
          nullable: true
        tiers:
          type: object
          additionalProperties:
            type: integer
        sites:
          type: array
          items:
            $ref: '#/components/schemas/SiteSummary'
      required:
      - id
      - name
      - scanned_at
      - sites
      - slug
      - tiers
    ProviderEnum:
      enum:
      - cloudflare
      type: string
      description: '* `cloudflare` - Cloudflare'
    PurposeEnum:
      enum:
      - prod
      - test
      type: string
      description: |-
        * `prod` - Prod
        * `test` - Test
    Question:
      type: object
      properties:
        id:
          type: string
        prompt:
          type: string
        kind:
          type: string
        default:
          nullable: true
        choices:
          type: array
          items:
            type: string
        secret:
          type: boolean
      required:
      - id
      - kind
      - prompt
      - secret
    Readiness:
      type: object
      properties:
        scanned_at:
          type: string
          format: date-time
          nullable: true
        modules:
          type: array
          items:
            type: string
        summary:
          type: object
          additionalProperties: {}
        blockers:
          type: array
          items:
            type: object
            additionalProperties: {}
        warnings:
          type: array
          items:
            type: object
            additionalProperties: {}
        advice:
          type: array
          items:
            type: object
            additionalProperties: {}
        pending_sandbox:
          type: array
          items:
            type: object
            additionalProperties: {}
      required:
      - advice
      - blockers
      - modules
      - pending_sandbox
      - scanned_at
      - summary
      - warnings
    RollbackResult:
      type: object
      properties:
        deployment_id:
          type: integer
        original_id:
          type: integer
        status:
          type: string
      required:
      - deployment_id
      - original_id
      - status
    SeverityEnum:
      enum:
      - p1
      - p2
      - p3
      type: string
      description: |-
        * `p1` - P1
        * `p2` - P2
        * `p3` - P3
    SiteEdgeOwner:
      type: object
      description: PATCH /api/v1/sites/{id}/ — {edge_owner} only (design note §7 I-edge).
      properties:
        edge_owner:
          $ref: '#/components/schemas/EdgeOwnerEnum'
      required:
      - edge_owner
    SiteSummary:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        domain:
          type: string
        latest_manifest_version:
          type: integer
          nullable: true
        manifest_current:
          type: boolean
          nullable: true
        cert_refusal:
          allOf:
          - $ref: '#/components/schemas/CertRefusal'
          nullable: true
      required:
      - domain
      - id
      - latest_manifest_version
      - manifest_current
      - name
    StateEnum:
      enum:
      - open
      - acked
      - resolved
      - accepted
      type: string
      description: |-
        * `open` - Open
        * `acked` - Acked
        * `resolved` - Resolved
        * `accepted` - Accepted
    Transition:
      type: object
      properties:
        action:
          $ref: '#/components/schemas/ActionEnum'
        reason:
          type: string
          default: ''
          maxLength: 256
      required:
      - action
    WizardState:
      type: object
      properties:
        questions:
          type: array
          items:
            $ref: '#/components/schemas/Question'
        answered:
          type: object
          additionalProperties: {}
        blocking:
          type: array
          items:
            type: object
            additionalProperties: {}
        warnings:
          type: array
          items:
            type: object
            additionalProperties: {}
        can_materialize:
          type: boolean
      required:
      - answered
      - blocking
      - can_materialize
      - questions
      - warnings
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: sessionid
