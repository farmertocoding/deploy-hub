modules: django
summary: ✖ blocker:1  ⚠ warning:2  • advice:0  ▸ pending_sandbox:3  ✓ ok:19

── BLOCKER (1)
  ✖ core.secret-scan: Secrets detected in the scanned tree
      .env: .env file present in the scan tree
      Dockerfile:43: [heuristic] hardcoded secret_key value
      frontend/scripts/drill/README.md:25: [heuristic] hardcoded django_superuser_password value
      frontend/scripts/drill/redteam/01_rbac_money.mjs:10: [heuristic] hardcoded staff_password value
      frontend/scripts/drill/qa/03_regressions.mjs:27: [heuristic] hardcoded customer_password value
      frontend/scripts/drill/qa/05_archive_results.mjs:36: [heuristic] hardcoded staff_password value
      frontend/scripts/drill/qa/06_admin_shell.mjs:34: [heuristic] hardcoded staff_password value
      frontend/scripts/drill/qa/08_redis_outage.mjs:72: [heuristic] hardcoded customer_password value
      frontend/scripts/drill/qa/08_redis_outage.mjs:522: [heuristic] hardcoded password value
      frontend/scripts/drill/qa/08_redis_outage.mjs:684: [heuristic] hardcoded password value
      frontend/scripts/drill/qa/09_consent_and_lifecycle.mjs:51: [heuristic] hardcoded customer_password value
      frontend/scripts/drill/qa/10_destinations_and_reconciliation.mjs:37: [heuristic] hardcoded customer_password value
      backend/.env: .env file present in the scan tree
      .github/workflows/ci.yml:136: [heuristic] hardcoded secret_key value
      .github/workflows/ci.yml:277: [heuristic] hardcoded secret_key value
      .github/workflows/ci.yml:286: [heuristic] hardcoded db_password value
      .github/workflows/ci.yml:339: [heuristic] hardcoded django_superuser_password value
      
      Also in test material (not blocking):
      backend/accounts/tests/test_password_reset.py:104: [heuristic] hardcoded password value
      backend/accounts/tests/test_profile.py:106: [heuristic] hardcoded password value
      Fix: Move secrets to the vault / environment injection. A .env file in this tree ships with a deploy of it, and is a leak as well if it is committed — check `git status`, add .env to .gitignore, and rotate anything that was committed: it stays in git history until you do.
      
      [proof] lines matched a published credential format — a GitHub token, a PEM block, an AWS key id — and are not guesses. [heuristic] lines are a secret-shaped name assigned a high-entropy literal: real most of the time, and worth a look before you decide.

── WARNING (2)
  ⚠ core.digest-pins: Base images not pinned by digest (§6.8)
      Dockerfile:9: FROM node:24-alpine is not digest-pinned
      Dockerfile:17: FROM python:3.12-slim is not digest-pinned
      Dockerfile:65: FROM caddy:2-alpine is not digest-pinned
      Fix: Pin each FROM to an @sha256: digest so builds cannot silently change under a moving tag.
  ⚠ core.declaration-file: deployhub.yaml is present but declarations are disabled
      This repo carries a deployhub.yaml. The declared-test-material mechanism it belongs to is deferred to its own phase, so this scan did not parse the file and no claim in it changed anything: every finding under a declared path is reported at its full tier, exactly as it would be if the file were not here. The file is otherwise ignored — and it is scanned like any other file in the tree, so a credential written into it is a finding of its own.
      Fix: Nothing to do for the deploy: no result above was downgraded. Read this as a correction to what the repo expects — if a tree was declared in deployhub.yaml in the belief that its findings would stop blocking, they are blocking, and either the findings or that expectation needs attention. Leaving the file in place is fine; it will be honored again when the mechanism returns with the threat model it is waiting on.

── PENDING_SANDBOX (3)
  ▸ django.check-deploy: [sandbox] django.check-deploy
      Runs off-Hub (review3 §M1): python manage.py check --deploy
      Fix: Django's own deploy checklist; needs the app importable, so sandbox.
  ▸ django.migrations-check: [sandbox] django.migrations-check
      Runs off-Hub (review3 §M1): python manage.py makemigrations --check --dry-run
      Fix: Detects model changes missing a migration; imports the app, so sandbox.
  ▸ django.collectstatic: [sandbox] django.collectstatic
      Runs off-Hub (review3 §M1): python manage.py collectstatic --noinput --dry-run
      Fix: Proves static collection succeeds before a deploy depends on it.

── OK (19)
  ✓ core.lockfile: Dependency manifests are locked
  ✓ core.gitignore: .gitignore covers the basics
  ✓ core.tests-exist: Test files found
  ✓ core.healthz: Health endpoint detected
  ✓ core.exposure-auth: Authentication indicators found
      names.mjs: signin
  ✓ django.settings-shape: Single settings module, env-driven
      settings.py reads its configuration from the environment.
  ✓ django.debug-hardcoded: DEBUG is not hardcoded True
      No prod-reachable settings file pins DEBUG = True.
  ✓ django.secret-key-literal: No secret literals in settings
      SECRET_KEY and friends come from the environment.
  ✓ django.allowed-hosts: ALLOWED_HOSTS is configured
      Set (literal hosts or env-driven) in prod settings.
  ✓ django.db-engine: Database engine
      Engine: postgresql.
  ✓ django.static-root: STATIC_ROOT is set
      collectstatic has a destination.
  ✓ django.security-settings: HTTPS security settings present
      All of: SECURE_SSL_REDIRECT, SESSION_COOKIE_SECURE, CSRF_COOKIE_SECURE, SECURE_HSTS_SECONDS, SECURE_PROXY_SSL_HEADER
  ✓ django.csrf-trusted-origins: CSRF_TRUSTED_ORIGINS is set
      Cross-origin POSTs from the SPA/domain will pass.
  ✓ django.dependency-manifest: Dependency manifest: requirements.txt
      source: requirements (requirements*.txt).
  ✓ django.deps-pinned: Dependencies are pinned
      All requirements entries pin exact versions.
  ✓ django.runtime-versions: Runtime versions recorded
      Python: not declared (requires-python); Django: 5.2 (dependency spec).
  ✓ django.server-mode: ASGI serving detected
      ASGI (daphne): daphne in dependencies. Service argv: daphne -b 0.0.0.0 -p 8000 config.asgi:application.
  ✓ django.sidecars: Sidecar processes recorded (1)
      scheduler (long_running): python manage.py run_auction_scheduler — each becomes a manifest components.jobs entry.
  ✓ django.runserver: No runserver in container files
      Dockerfile/compose entries use a real app server.

manifest draft: service=django strategy=blue_green exposure=public jobs=1 volumes=0
wizard: 3 question(s) pending
