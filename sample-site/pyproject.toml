[project]
name = "sample-site"
version = "0.1.0"
description = "Minimal Django/ASGI healthz fixture for deploy-hub T3"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2,<6.0",
    "uvicorn>=0.34,<1",
]
