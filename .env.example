# Copy to .env and fill. .env is gitignored — secrets never in git (§10).
# Generate values:  python -c "import secrets; print(secrets.token_urlsafe(48))"
HUB_SECRET_KEY=
POSTGRES_PASSWORD=
REDIS_PASSWORD=
