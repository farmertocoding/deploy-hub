# DECISIONS.md — decide and log, don't ask (build-process.md §6)

| id | date | decision | why | what would reverse it |
|---|---|---|---|---|
| D-001 | 2026-08-02 | Repo lives in `~/Documents/deploy-hub` (local folder), per Joseph. GitHub private repo required no later than Phase 2.5. | CI (GitHub Actions) is load-bearing from Phase 2.5; local is fine while the skeleton stabilizes. | Nothing — pushing to GitHub is additive. |
| D-002 | 2026-08-02 | TS client generation tool deferred until the frontend consumes the schema (Phase 0 build, not scaffold). Candidates: openapi-typescript + zod via openapi-zod-client. | §4.5 requires generated-not-hand-written mirroring; tool choice is swappable behind the generated-artifacts dir. | Either tool failing to express DRF warning contract. |
| D-003 | 2026-08-02 | Scaffold ships single-call login (password+TOTP in one POST) rather than a two-step ceremony. | Mockup-first; the session model is identical, so splitting later is a UI change only. | UX review finding in a Phase 0 round. |
| D-002r | 2026-08-03 | D-002 resolved: `openapi-typescript` for TS types + `openapi-zod-client` (schemas-only template, `--export-schemas`) for zod — both generated from the same `openapi.yaml`. Zodios client deliberately not used; friendly error copy lives in an RHF `errorMap`, never in the generated file. | Both tools express the DRF contract (regex, min/max, defaults) verbatim; schemas-only output avoids adopting a client framework in Phase 0. Guarded by `frontend/tests/zod-contract.test.ts` + `make check-generated` staleness gate. | Either generator failing to express a future serializer construct — swap behind `src/api/`, regenerate. |
| D-008 | 2026-08-09 | A committed dev-fallback secret whose NAME is reassigned in a prod-reachable settings file from a hard-fail env read (`environ[...]` subscript) tiers as **warning-with-context** (`django.secret-dev-fallback`), not blocker. Unguarded literals, and `.get(default)` fallbacks, stay blockers. Chosen by Joseph over always-Blocker and proof-dependent tiers. | Blocker means "will not deploy correctly"; the fail-loud guard is the fleet norm (inventory finding 5) and provably prevents shipping the literal. A scanner that blocks deployable projects trains the operator to bypass blockers. The warning copy still says the value lives in git history and must be rotated if the repo was shared. | A real incident where a "dev-fallback" key turns out to have protected production data — that would justify Option B (always-Blocker). |
| D-009 | 2026-08-11 | R4-11 WI-3: do **not** invent a wizard data-sensitivity question to implement SCAN-M4-EXPOSURE-AUTH's "Blocker when the wizard tags financial/personal data" clause. Remove the two `SCAN-M4-EXPOSURE-AUTH` markers, waive the requirement (WAIVERS.md, 2026-08-11), and keep the warning-half tests running unmarked. | The escalation needs an answer to read and no wizard question tags data sensitivity — the whole question set is domain/exposure/db/env/package/volume/worker shape. Adding one is a product decision (what counts as "personal"? does it gate materialize or only the report?) that belongs to the Architect and the wizard's own PR, not to a PR whose brief is making the *tests* honest; build-process.md §4 also forbids reaching green by widening scope under pressure. Auditing the clause turned up a second, unanticipated gap in the same requirement — the django module never calls `common_checks`, so `core.exposure-auth` fires for node-ts and the fallbacks but never for Django — which makes even the warning clause partly false and settles the marker question: no single test can honestly carry this marker today. A waiver states that; `verified` would not. | The wizard gaining a data-sensitivity tag (then implement the escalation and restore the markers), or the Architect ruling that `core.exposure-auth` belongs in the django module's check set — either one retires part of the waiver. |
| D-010 | 2026-08-11 | Architect ruling (SPEC-django-common-checks.md): the common core suite (`core.*`, seven checks) is composed into every scan report by the **registry** — `scanner.core.scan` runs `common_checks(<scan root>)` once per scan and merges module results, a module superseding a core result only by emitting the **same id** (precedent: node-ts `core.lockfile`). No module calls `common_checks` itself; inapplicability is expressed by a same-id override with an explaining detail, never by absence; `core.scan` raises on unknown `core.*` ids. This corrects D-009's stated radius: the django gap was never exposure-auth-only — django scan reports lacked **all seven** core checks, including `core.secret-scan`, so a Django repo with a committed `.env`/`AKIA…` key scanned clean while the identical Node repo blocked. | Per-module composition is the defect shape (three modules remembered the call, the fourth didn't) and provably double-emits all seven ids when two modules co-match (django+node-ts probe, spec §3.4); the suite is a property of the scanned tree, not a framework subtree (django re-roots to `backend/`); registry composition is behavior-preserving where behavior was right — the sample-node-site demo JSON is byte-identical. | A framework module needing the core suite run over a different root than the scan root (e.g. per-package monorepo scanning) would reopen the composition-point question. |
| D-011 | 2026-08-11 | `core.secret-scan` gains a second detection axis and labels every finding by confidence: `[proof]` for a match on a published credential format (AKIA, PEM block, `ghp_`, `sk_live_`, a connection string carrying `user:password@`, a docker `auth` blob that decodes to `user:password`), `[heuristic]` for a secret-shaped identifier assigned a high-entropy literal. Both stay at **blocker** tier for now. The security expert's review recommended demoting heuristic-only findings to `warning`; that is **deferred to Joseph**, not taken here. | Detection was entirely name-driven, so a committed `id_rsa`, `ghp_…` or `sk_live_…` scanned clean unless the variable holding it happened to be named helpfully — the value axis closes that and is genuinely proof rather than a guess. Labelling costs nothing and fixes the reviewer's real objection, which was that presenting proof and guesswork identically overstates the guesswork. Demoting is a different thing: it reduces what the gate refuses to deploy, and D-008 already settled that this project's tiering choices belong to Joseph, on the stated ground that a scanner which blocks deployable projects trains the operator to bypass blockers — the same argument runs the other way here, so it is his call, not a reviewer's and not the author's. Framework modules keep their own high-confidence blockers (`django.secret-key-literal`), so a committed Django key blocks either way. | Joseph choosing the demotion — a one-line tier change in `_check_secret_scan` plus the fixture expectations — or a real detection library (gitleaks/detect-secrets) landing behind the heuristic axis, which would make the distinction moot. |
| D-011r | 2026-08-11 | D-011 resolved by Joseph: heuristic-only secret findings **stay at blocker tier**. No code change — the shipped tiering is already blocker for both axes; this row closes the open decision. | Ruled with the demotion's cost on the table (a real secret in a heuristic-only form would deploy under `warning`). The `[proof]`/`[heuristic]` labels remain the answer to the reviewer's overstatement objection; strictness is retained until the heuristic's real-fleet noise is measured. | The phase-1 demo re-records (first run of the widened checks over the real fleet) showing heuristic noise high enough to train blocker-bypass — the D-008 argument — would reopen this with data. |
| D-012 | 2026-08-12 | A scanned repo may declare its own drill/QA trees as test material in a `deployhub.yaml` it checks in (`scanner.test_material`: `path` + mandatory `reason`). A declaration moves only `core.secret-scan`'s **heuristic-axis** findings under that path into a separate, non-blocking, labelled bucket; the `[proof]` axis, the `.env` handler and every other check are untouched there. The report **always** prints the claim and its reason (even when it downgraded nothing), a downgrade can never leave the check at `ok`, and the wizard raises a no-default confirm per declaration which the manifest records. Rejected by design: declaring the scan root, a path holding a manifest the scanner keys on, a stale path, a malformed file — each warns and downgrades nothing. | Ruled by Joseph after the fleet measurement: heuristic noise was not distributed but structural — one QA/red-team directory in SATURDAYS_site was 20 of 26 blocking lines (10 of 15 after N7). Round-6b had already ruled out the scanner *guessing* test material from directory names, because `spec`/`fixtures`/`e2e`/`cypress` also name production trees. So the knowledge has to come from the repo, in writing, with a reason — and the whole cost of trusting a repo about itself is paid back by making the claim impossible to hide: it is printed with its reason on every report, and the operator confirms it. The alternatives were an operator-side waiver, which does not travel with the repo and would not scale to partner repos, and keeping the tier, which leaves a known-false blocker training bypass — the D-008 argument. | A real detection library behind the heuristic axis making the tier distinction moot; or evidence that declarations are being written to hide real findings, in which case the confirm step becomes an approval with an audit trail rather than a question. |
| D-011r-closed | 2026-08-12 | D-011r's revisit question is **closed, not reopened**. With N5–N8 and D-012 landed, SATURDAYS_site — the noisiest repo in the fleet — reports **5 blocking heuristic lines** where the first measurement found 26, and 10 of the removed lines are declared, printed and confirmed rather than hidden. Heuristic-only findings stay at blocker tier. | The reopen trigger was "noise high enough to train blocker-bypass". The noise was measured three times and fell to a level an operator can read: E-invoice 1, hr-saas-starter 6, SATURDAYS_site 5, TAKKO 0. Crucially, the remaining lines are the kind that *should* be read before a deploy (a Dockerfile fallback key, four CI-workflow values, a prod-smoke env file), not a wall of fixtures. Every reduction along the way was a correctness fix with an adversarial pass behind it, not a tier demotion — the gate refuses exactly what it refused before, minus findings that were provably false. | A new fleet member re-raising blocking heuristic counts into the tens with findings an operator would dismiss unread — the measurement is cheap and repeatable, so this reopens with data or not at all. |
| D-012r | 2026-08-12 | D-012 **corrected** by Joseph on the security expert's round-7 veto (R7-1), spec `docs/spec-r7-enforce-declaration-acceptance.md`: **no acceptance, no downgrade.** A declaration is a *request*. `core.secret-scan` keeps the three buckets, the labels and the always-printed header, but the tier no longer drops on account of a declaration — declared findings block, the section header says the downgrade was *requested* and blocks until accepted, and the check publishes a structured `acceptance: {questions, blocking_only_declared}`. The wizard owns the grant: every `scanner.test_material.*` confirm is now required (unanswered refuses materialization), a blocker whose entire blocking case is declared stops blocking only when every one of its confirms is answered `True`, `blocking_only_declared: false` is unconditional so acceptance can never clear a `[proof]` line / `.env` file / undeclared heuristic line, and the frozen manifest's `declared_test_material` is rebuilt from the ANSWERS — refusals recorded under `declared_test_material_refused`, never dropped. | D-012 rested on three controls and shipped one. The claim was printed (real); the confirm was raised but was not in `REQUIRED_IDS` — it could not be, its id carries the declared path and that set is static — so unanswered blocked nothing; its answer went to `body["module_answers"]` and was **read by no code**, so `False` and `True` produced identical deploys under a `fix_hint` telling the operator that refusing "is how you say the declaration is wrong"; and `manifest_draft["declared_test_material"]` was frozen straight from the scan draft, so the append-only audit artifact asserted an acceptance that may never have happened. The downgrade itself was applied at scan time from a file the scanned repo writes — a repo unilaterally downgrading its own blockers, which is precisely the trust D-012's ruling declined to grant without an operator's act. Three reviewers found it independently in round 7. The fleet effect is one artifact: SATURDAYS_site's record moves in the section header and gains the declaration legend on the blocker path (R7-8); the other three fleet records and the `sample-node-site` fixture are byte-identical, verified with `cmp`. | Evidence that the confirm is being answered `True` unread — the D-012 reopen trigger one step on, and the remedy there is an approval with an audit trail and a second pair of eyes, not a question. A per-finding `acceptance` array (§F2) would also supersede the coarse `blocking_only_declared` flag. |
| D-012r2 | 2026-08-12 | D-012r's remedy corrected on a second round-7 veto (`acceptance-id-ignores-reason-and-survives-rescan`): the declaration confirm id is keyed on the declaration's **content**, not on its position. `confirm_question_id(path, reason)` returns `scanner.test_material.<path-slug>--<16 hex of sha256(path + "\n" + reason)>`; the **index is gone from the key**. Editing the path or the reason yields an id nobody has answered, so `missing_required` fires and the proven fail-closed machinery re-blocks; re-scanning an unchanged declaration asks the same question and keeps its answer. A sibling scrub, `wizard.service.scrub_orphaned_declaration_answers`, drops dead confirm rows on every write — explicitly hygiene, never the boundary. Report copy hedged: for `blocking_only_declared: False` the legend now says accepting clears those lines "and no more". | Two attacks, both demonstrated end to end against a real preflight/materialize with a database. REASON SWAP: accept drill for "deliberate fake credentials", repo rewrites the reason to "ACTUALLY covers prod secrets now" with the same path and index, re-scan — same id, stale `True` matched, preflight cleared, and manifest v2 froze the new reason as accepted. `_read_entry` refuses an entry with no reason because "a downgrade with no stated reason is not reviewable"; a reason mutable underneath a granted acceptance is worse than none, since it comes with a signature on it. INDEX ROUND-TRIP: prepend a declaration (drill → index 2, re-blocks) then remove it (drill → index 1) and the orphaned `True` re-applied. Dropping the index is what closes the second by construction rather than by sweep — an index is a slot, and a slot is somewhere an orphan can wait; with content-only keys there is no old slot, so nothing depends on the scrub having run between two events. 16 hex = 64 bits because the attacker controls both inputs and aims at one stored id, making this a second preimage (2^n, not 2^(n/2)): 32 bits is minutes on a laptop, 64 is out of reach, and the full digest would swamp an id the operator reads and that must fit CharField(max_length=128). | A per-finding `acceptance` array (§F2) would re-key this anyway. Evidence that operators re-accept an edited declaration without reading the diff would move the remedy from "ask again" to "show what changed". |
| D-013 | 2026-08-12 | `core.secret-scan` reports **`warning`, never `ok`, about a tree it could not fully read** (round-7 R7-2). Directories the walk cannot open and files whose bytes cannot be read are collected by `fallbacks._iter_files`/`_read_text` into one skip list; the check names them (capped at 10, with the total count) and says the results may be incomplete. If findings exist the list rides the detail; if nothing else was found the tier is `warning` with its own title. **The ruling on scope:** what joins the list is *the filesystem refused*, not *a directory* — an unreadable file is the same blindness one level down — while a file the scanner DECLINED to read (binary, oversized, undecodable) does not join it and does not warn. | A permission-denied `prodcfg/` holding a real AWS key turned the same tree from `blocker` into `tier: ok`, "No committed secrets found", empty detail: the `OSError` was swallowed with a bare `continue`, so a subtree that was never opened was reported as clean. A security gate must degrade to an honest error, never to a pass. The file/directory line is drawn at *chose not to look* versus *was not allowed to look* because the second is unbounded and repo-controlled while the first is bounded by rules in this repo — and warning on every binary file would fire the check on every scan, which is how a warning is read as off. Fleet effect: none — all four fleet trees and the fixture have zero unreadable paths, and all five demo records are byte-identical. | A repo where the scanner legitimately cannot read a subtree on every scan (a mounted artifact store, a root-owned build cache) making the warning permanent noise — the remedy then is an explicit, reviewed exclusion in `deployhub.yaml`, not a silent skip. |
| D-014 | 2026-08-20 | Standing model assignment is **Grok 4.6 for every judgment seat** (Architect, Implementer, Security, QE, SRE, UX, Researcher, Adversarial verifier). Cheap mechanical roles stay Haiku. The 2026-07-30 rule ("Opus 5 writes code; Fable 5 makes decisions and does QA") is superseded. The delegation rule is restated as a **session split**, not a model split: a reviewer never writes the code it reviews, even when both seats run `grok-4.6`. | Joseph's 2026-08-20 override: the old plan named two models this session cannot spawn; grok 4.6 is the available model that covers both writing and QA. Role separation is load-bearing; model differentiation is not. | A later session that can actually spawn distinct writer/QA models, and a reason to split them again. |
| D-015 | 2026-08-20 | J-1 closed: **T2 is an sshd+systemd container with inner docker on vfs; Multipass is not needed sooner.** Pipeline steps 1–5 (build-on-target, ship, migrate, start green, healthz) are T2-testable as a protocol. ufw/fail2ban, overlay2, reboot, and host sysctl stay T3 (Phase 2.5), as §G already said for the hardening truth test. `hub-test-target` is Ubuntu, systemd as PID 1, privileged + cgroupns=host, multi-arch or locally built, docker `storage-driver=vfs`. Do not bind the Hub's docker.sock into the target. Record: `docs/j1-t2-fidelity-spike.md`. | Probed on Docker Desktop 20.10.17 / arm64 / cgroup v2. sshd banner, systemd `running`, `sshd -t` + drop-in + `systemctl enable --now ssh` all real. Default overlay inside systemd-ubuntu failed `overlay … invalid argument`; vfs built and ran an image. Official `docker:20.10-dind` overlay worked as a control, so nested docker is possible — the failure is overlay-on-overlay under apt `docker.io`, not "containers cannot run docker". ufw reported active (false-green); fail2ban did not become active. `jrei/systemd-ubuntu:22.04` has no arm64 manifest. | Inner docker/vfs failing to run a real site image that overlay2 on a VM would run — then step 1–5 of T2 shrinks to sshd+systemd and the pipeline protocol moves to T3. A CI runner where nested overlay2 works would let T2 drop vfs. |
| D-016 | 2026-08-20 | Phase 1 **real-repo demos are SATURDAYS_site and TAKKO only.** E-invoice and hr-saas-starter stay in the §J7 inventory (shape checks, regression fixtures) but are not demo artifacts and are not re-scanned for P1-SCAN-DEMO. `sample-node-site/` remains the in-repo node-ts fixture (Q7); it is not a fleet demo. The older `~/SATURDAYS` Django tree is not the demo — `SATURDAYS_site` is. | Joseph, 2026-08-20: "only use SATURDAYS and TAKKO for demo." Two real Django+SPA trees are enough to prove the django module against production-shaped repos; the other two were inventory, not the exit demo. Trading-repo demo stays unmet until that folder is connected; Q7 covers node-ts in CI. | Joseph naming another real repo as a demo target, or dropping SATURDAYS_site/TAKKO. |
| D-017 | 2026-08-20 | D-012 is **not** re-landed in Phase 2. `SCAN-DECLARED-TEST-MATERIAL` and `SCAN-DECLARED-GUARDS` bump from phase 2 to **phase 4** (security suite; threat model is the first page of that design note). Registry edit is sensitive-path; parked `tests/test_scanner_declarations.py` keep their markers. Ids are not retired; `text:` is unchanged. | Joseph parked the mechanism "as its own phase with a threat model written first." Option (a) (Task 0 = threat model + re-import) would mix scanner-trust work into the deploy wave and contradict that parking. Option (b) is what makes `check.py --phase 2` not blocked by parked scanner work. | A written threat model plus a decision to re-land D-012 before Phase 4; or evidence that Phase 2 cannot close without the declaration mechanism. |
| D-018 | 2026-08-20 | `Transport.probe(argv)` is the read-only seam. `run` / `put` stay mutating; `get` / `probe` do not. `FakeTransport.mutating_calls()` is **unchanged** (`run` and `put` only). Using `run` for an inspect is fail-closed: run-twice goes red. | Run-twice tests need a way to inspect without counting as mutation. Mixing inspect into `run` would make idempotency tests fail or go green for the wrong reason. | A Transport where inspect cannot be separated from mutate (then `mutating_calls()` would need a different definition); or `get` becoming a write. |
| D-019 | 2026-08-20 | Task order vs the brief's sketch: pull **`hub-test-target` forward** (SSH and pipeline T2 need it); land **heartbeat with the pipeline skeleton**; land **volumes/recreate/readiness with steps 4–5**, not after 1–9. Remainder follows the brief. See `docs/plan/phase-2-tasks.md`. | SSH and pipeline T2 cannot wait for the later slot; heartbeat is needed with the skeleton; volumes/recreate/readiness belong with the steps that use them. | Evidence that `hub-test-target` can wait, or that heartbeat/volumes can land later without blocking T2 of steps 1–5. |
| D-020 | 2026-08-20 | Protective cut — see design note §6 and the task plan. Milestone that MUST ship is unchanged from the brief (deploy to own machine: CI half + TAKKO real-world half). Phase 2b slips without failing the MUST demo: git polling, windowed `deploy_policy`, reconciler brakes, collector, `hub-upgrade.sh`, env-lifecycle UI, DB/backup Beat jobs, UX-F8 seed polish, REL-P2 24h duration. Those ids stay phase 2; taking the cut means a waiver or a phase bump before `check.py --phase 2` can close. | Protect the MUST demo from slipping items that are still phase-2 in the registry. | Taking those 2b items into the MUST line; or bumping those ids' phase before the cut is taken. |
| D-001r | 2026-08-21 | GitHub private repo is `farmertocoding/deploy-hub` (`git@github.com:farmertocoding/deploy-hub.git`). Local path is `/Users/j/j/deploy-hub`. `origin` tracks `master` (default) and `phase-2`. | D-001 said pushing is additive; Linux review-round needed a remote. | **Partly superseded by D-022.** |
| D-022 | 2026-08-21 | GitHub is the **git remote only**. Mechanical review-round host is a **Cursor Cloud Agent (Grok 4.6)** on Ubuntu, running bare `make review-round`. GitHub Actions is **disabled** on `farmertocoding/deploy-hub`. Do not post GitHub Checks; report in the Cloud Agent run (and a PR comment when there is a PR). The agent must not edit code or open fix PRs — reporter only. T2 stays `make test-t2` on a machine with docker, not this bot. | Actions billing blocked hosted runners; Free private repos cannot protect `master`; Joseph: use GitHub as repo, Grok/Cursor agent for the gates. Cloud VMs are Ubuntu, which is the Linux host Darwin pytest is not. | Turning Actions back on; or a non-Cursor Linux runner that still is not GitHub Checks. |
| D-023 | 2026-08-21 | Host of record = **local Multipass** + `make test-t3` / `make nightly`. GHA QEMU is an optional dormant recipe. Phase exit must not require paid Actions (D-022). | Actions billing is broken (D-022); Darwin / Cloud Agent may lack Multipass; local Multipass is the nightly host. | Turning Actions back on as a required gate; or a non-local T3 host becoming the record. |
| D-024 | 2026-08-21 | A `tier: t3` req is verified only by a **passed `@pytest.mark.t3` test**. T1 siblings, skips, and xfails never verify it. `check.py --phase 2.5` is the phase gate; `make review-round` uses `--exclude-tier t3`. | Skipped T3 used to look like coverage; a T1 sibling must not green a Multipass-only obligation. | A later ruling that T3 may be proven by T1 protocol tests. |
| D-025 | 2026-08-21 | T2 tries the real `sample-node-site` image on vfs; alpine COPY stub + existing PIPE-S4 waiver stay if vfs cannot. Do not silently drop the waiver. | vfs overlay-on-overlay already failed for the real image (D-015); dropping the waiver without a green T2 build would be fiction. | T2 building the fixture image on vfs (or a runner where nested overlay2 works). |
| D-026 | 2026-08-21 | REL-P2 24h waiver **stays**. 2.5 ships a Beat drill + short nightly Hub-down stub. `--restart unless-stopped` lands so a later 24h is possible. | 24h live Hub-down is still unproven; a stub that claims 24h would be the same defect the waiver records. | A dated 24h drill record that retires `REL-P2-HUB-DOWN-SITES-UP+verify-demo+24h-unproven`. |
| D-027 | 2026-08-21 | `hub-upgrade.sh` hold-through-build drain is **optional**. Named refuse-immediately remains the HARD-Q8 proof; existing TOCTOU waiver may stay. | Protective cut (D-031); refuse-immediately is already tested. | A hold-through-build that waits, then still refuses after timeout, with tests that retire the TOCTOU waiver. |
| D-028 | 2026-08-21 | Test-plane DNS client ≠ Phase 3 Cloudflare adapter. Test-mode-only; refuse non-allowlisted zones. | Product Cloudflare adapter is Phase 3; this phase may add a test-plane-only DnsProvider. | Landing the Phase 3 product adapter behind the same module. |
| D-029 | 2026-08-21 | `check.py --phase` becomes float (`2.5`). Review-round conformance does not demand a Multipass run. | argparse `type=int` made `--phase 2.5` impossible; Cloud Agent review-round must stay T1 (D-022). | A later phase scheme that is not a half-step float. |
| D-030 | 2026-08-21 | `sample-site/` is absent; 2.5 lands a minimal Django/ASGI fixture so the T3 first leg is not fiction. | T3 deploys both fixtures; without `sample-site/` the Django half is a story. | The fixture landing (Task 5) or a ruling that node-site alone is enough. |
| D-031 | 2026-08-21 | Protective cut — design note §6. MUST = Tasks 0–4, 6–11, 13–14, 16. Tasks 5, 12, 15 may slip with an honest waiver (alpine / LE-absent / drain-TOCTOU). | Protect the MUST Multipass nightly from slip items that are still named in §3. | Taking alpine/LE/drain onto the MUST line; or bumping those ids' phase before the cut is taken. |
| D-032 | 2026-08-22 | Phase 3 ships DNS/TLS + alerting/monitoring + map/operator-UX. App-log viewer (§E3), scheduled-jobs UI (§E8/§N7), first-run checklist (§F3), the backup/restore surface (§E5) and — per the panel — adopt-existing-site move to Phase 3b; the Scribe amends the §I Phase-3 line at exit. | The phase-3 design note's scope line, r2-amended by the three-seat panel's unanimous APPROVE-WITH-CHANGES; the moved items each lacked either a spec or a phase-3 dependency. | The Scribe's §I amendment restoring one of the moved items to Phase 3, with the spec holes it left with answered. |
| D-033 | 2026-08-22 | `DnsZone` (under a `DnsAccount`) is the single DNS-zone identity, unique on `(provider, name)`; `providers/registry.py::dns_provider_for(zone)` is the only construction path. The `HUB_TEST_ZONE_SLUGS` / `HUB_TEST_DNS_ZONE` dual namespace is retired behind it; `HUB_TEST_ZONE_SLUGS` survives. | Two zone namespaces (2.5 parked finding) meant an allowlist check could pass against one while the client was built from the other; one identity with one construction path closes that seam. | A provider whose zones cannot key on `(provider, name)`, or a second construction path becoming genuinely necessary — either reopens the identity question. |
| D-034 | 2026-08-22 | (Amended, panel ruling 1, 2-1.) One Cloudflare client: `providers/cloudflare.py` is the product adapter AND the client the credentialed T3 leg drives, under `HUB_TEST_MODE`, against an allowlisted `purpose=test` zone — D-028's stated reversal condition. The single-client concession is bought with mandatory hardening: three separate credential refs on `DnsAccount` (dns / edge / Origin-CA); synchronous fail-closed scope enforcement at construction — Bearer only, active token, and an exact one-zone authorization probe — so no code path holds an over-scoped client; the daily audit is defense-in-depth, not the enforcement; and test-zone construction stays triple-keyed (`HUB_TEST_MODE` + `purpose=test` + allowlist). | Two clients (D-028's shape) meant the tested client was never the shipped client; the panel ruled the fidelity worth more than the isolation once construction itself refuses over-scope. | A defect where the shared client leaks prod scope into the test plane despite the triple key — that re-splits the clients on the spot. |
| D-035 | 2026-08-22 | (Amended, panel ruling 2, 3-0.) Proxied sites get Cloudflare Origin certificates issued and pushed by the Hub; unproxied public sites get a named refusal surfaced as a Finding and a Sites-screen state, not Caddy DNS-01. SEC-B2's two clauses split: `SEC-B2-NO-TOKEN-ON-TARGET` is proven this phase by a scan of every target-bound surface (put payloads, run argv, env files and snapshots, Celery kwargs, DeploymentArtifacts, generated config text, recorded Transport calls); `TLS-B2-HUB-DNS01-UNPROXIED` is registered at phase 4; the full-text id carries no marker and a clause-scoped waiver until both clauses hold. | A marker on the full text would claim a DNS-01 path the phase does not build (the SCAN-M4 precedent); a bare exception on unproxied sites would be a refusal the operator never sees. | Hub-central DNS-01 landing (phase 4) retires the refusal state, the clause waiver, and restores the full-text marker. |
| D-036 | 2026-08-22 | The pager is a provider seam (`Pager`/`providers/ntfy.py`/`FakePager`), not a Transport effect. Distinct Hub / healthchecks / per-target publisher identities; subscriber read access is its own protected credential; decommissioning a target revokes its publish token. Bodies scrubbed and minimized; break-glass only in the email copy, advisory-only. | One shared topic secret let any compromised target read, spoof, or flood every alert (review3 §M3); a seam with per-identity tokens makes one compromised host identifiable and revocable. | ntfy proving unreliable in practice — the recorded fallback is Pushover, same one-call publish, protocol unchanged (alert-protocol §6). |
| D-037 | 2026-08-22 | One alert rules table (`monitor/alert_rules.py`) transcribed from alert-protocol §2; `classify()` raises `UnclassifiedAlert` on an unknown kind. Every §1 delivery behaviour (unacked repeat, P2 grouping, digest, weekly rollup) has a named module and Beat entry, so no row of the protocol is owned by nobody. | An unregistered alert kind that ships is an alert that never fires at the right severity; a delivery behaviour with no named owner is implied behaviour nobody implements. | alert-protocol §2 gaining per-alert configurability — a config surface would move the rules out of a hard-coded table into validated data. |
| D-038 | 2026-08-22 | `Finding` is the persistence for every alert (§F2 + alert-protocol §7); hysteresis/flap/storm counters live on `AlertState` keyed by the same fingerprint so the Finding model stays §F2-shaped. | Counters on Finding would bloat the operator-facing model with engine state; a second fingerprint would let an alert and its Finding drift apart. | §F2 gaining alert-specific fields by ruling — then the counters merge and `AlertState` folds in. |
| D-039 | 2026-08-22 | (Panel ruling 3, 2-1.) MON-DEADMAN-EXTERNAL and PROV-J7-COMPOSE-AWARE-ADOPT convert `verify: checklist` → `verify: test`; neither has an honest gate to name and R4-9 forbids inventing one. Conditions: a failed dead-man POST files its own Finding, and the external receiver's registration is a dated artifact in the phase-3 demo record. | A `checklist` req with no gate is uncovered forever or waived forever; both behaviours are now testable this phase, so `test` is the honest verify kind. | An honest Makefile/CI gate existing for either behaviour — checklist would then be legal again, though no longer preferable. |
| D-040 | 2026-08-22 | (Amended, panel ruling 4, 3-0.) `SEC-F5-T1-HARDWARE-TOUCH` is registered at phase 4 in Task 0; `UX-F5-T2-T3-FRICTION` carries the clauses Phase 3 can prove; the full-text `UX-F5-ACTION-TIERS` carries no marker and a clause-scoped waiver while the hardware clause is unbuilt — the SCAN-M4 shape, so the split cannot silently collapse. | WebAuthn/hardware step-up is Phase 4 (SEC-A2); a full-text marker this phase would claim a hardware clause no test can reach. | The hardware clause landing with the Phase 4 WebAuthn work — the waiver retires and the full-text marker goes back on. |
| D-041 | 2026-08-22 | Map v1 is plain SVG + a list-view toggle; map "zone" nodes are `NetworkZone`. The §J3 library choice stays open until a 30-node prototype needs it. | A library adopted before the graph outgrows plain SVG is a dependency chosen on speculation; `DnsZone` is a DNS-provider object with no place on a topology graph. | A 30-node prototype that plain SVG cannot render legibly — that spends the §J3 decision. |
| D-042 | 2026-08-22 | REL-P2's 24 h waiver stays. The hub-down drill gets a real external prober at the §G 30-min form, `due_at` rows so `find_missed` works in prod, and a defined no-eligible-site outcome (`SKIPPED` + reason + P2 Finding) so nightly stays honest and green on a siteless host. | 2.5 parked findings I5+M2: the drill never had a prober and nothing wrote `due_at`, so `find_missed` was inert in prod; an undefined siteless outcome would force nightly red or a fictional pass between T3 sessions. | A dated 24 h drill record — that retires `REL-P2-HUB-DOWN-SITES-UP+verify-demo+24h-unproven` (D-026's condition, unchanged). |
| D-043 | 2026-08-22 | Credential-gated Cloudflare/LE work keeps the D-031 shape: absent token → `skipped-only` + dated waiver, never a T1-sibling green; the LE waiver gains the self-refusing probe (2.5 finding M4). | A waiver that does not refuse itself when its stated condition stops holding is fiction with a date on it — M4 found exactly that shape on the LE line. | Test-zone credentials present on the host of record — the first credentialed run retires the skip and the waiver together. |
| D-044 | 2026-08-22 | (Amended, panel ruling 5.) Protective cut — design note §6. Task 15 (adopt) leaves the MUST line for Phase 3b with its five spec holes recorded in §1.12; Task 14 (map v1) stays MUST and is the named first slip candidate if the phase runs long. | Five open questions answered mid-implementation is how adopt would ship wrong; map v1 is real operator value but the one MUST item whose absence fails no other task. | Taking adopt back onto the MUST line with the §1.12 holes answered; or the phase running long enough to spend the map-v1 slip. |
| D-045 | 2026-08-22 | (New, r2.) One attention stream: `findings` is the canonical realtime topic; the Phase-0 `alerts` topic remains authorized as a deprecated alias for one phase (same payload, asserted by test) and the simulation seed moves to `findings`. | Retiring `alerts` outright this phase would break P0-AUTHZ-TOPIC and the seed for no operator benefit; an alias with an asserted-identical payload costs one test. | Phase 4 retires the alias on schedule; keeping it longer needs a new decision. |
| D-046 | 2026-08-22 | (New, r2.) Cloudflare zone authorization is established by the zone-set probe, not by `/user/tokens/verify`: verify returns `{result.id, result.status}` and no scope list, so it can only prove the credential is a live token. `GET /zones?per_page=50` returning exactly the expected `provider_zone_id` is what proves the token is not over-scoped, and a Global API Key is refused by header shape before either call. | Trusting verify alone would let an account-wide token construct a client for one zone while holding write on every zone the account owns. | A Cloudflare API that returns a token's policy set to the token itself. |
| D-047 | 2026-08-23 | Protective cut — design note §1.2. MUST = bind + plant + F3 + adopt (a)–(e). App-log, jobs UI, DNS-01, backup UI, LE-staging, 24 h Hub-down are out. MUST start is `adopt_flow` + `desired["dns"]` from `dns_provider_for`. Task 8 HTTP may slip. | Phase 3 shipped DNS/TLS and Settings-connect but never bound `Site.dns_zone` or planted Origin-CA, and adopt still had five §1.12 holes; pulling DNS-01 / app-log / jobs UI / backup UI / LE-staging / a 24 h Hub-down onto this wave would miss the operator path that makes a public site deployable. | Taking a cut item onto the MUST line with its spec holes answered; or bumping a MUST id off 3.5 before the demo. |
| D-048 | 2026-08-23 | V5 compression for adopt: one Site; no multi-container Site model. | A compose stack is not a new multi-container Site. Classify into the existing manifest (one service + optional static_route + optional jobs + optional cache pointer). Two unrelated public services that cannot compress refuse with a Finding. | A ruling that adopt needs a multi-container Site the data model does not have. |
| D-049 | 2026-08-23 | Compose source is the §A4 clone / `local_path`. Paste refused. `live_compose_path` is an explicit adopt-flow argument; unset → no drift check; no disk walk; `/srv/sites/{slug}` is not a default. | Live-host discovery and operator paste are unbounded; the git/local tree is already the Hub's source of truth, and `/srv/sites/{slug}` is the post-Hub layout. | A ruling that adopt must read compose off the live host by default. |
| D-050 | 2026-08-23 | Temp name lives in `Site.dns_zone`. 8hex = `secrets.token_hex(4)` in `CheckRun.results.temp_name` at first upsert. `results` closed schema includes `site_id`. Cleanup = `adopt_flow.cleanup` + Beat `adopt-temp-reaper` (24 h from `results.started_at`). Reaper reconstructs via `dns_provider_for(site.dns_zone)` / seams; never stores a token. No adoption zone. Mesh-only: no temp DNS. | A dedicated adoption zone splits identity; persisting a token for the reaper is the SEC-B2 leak; mesh-only has no public name to park. This 24 h is an abandon TTL, not a Hub-down and not a REL-P2 claim. | A second zone becoming necessary, or a CheckRun Site FK, or a stored reaper token. |
| D-051 | 2026-08-23 | Adopted data is point-in-place. Obtain `DATABASE_URL` from compose `environment`/`env_file` else `docker inspect` (db, else web) → `vault.put(kind=DATABASE_URL, owner_type=site, owner_id=site.pk)` → `_env_mapping_for_deploy` only. Missing → Finding, never `ensure_site_db`. Cache/redis = same pointer rule. Unmapped volume blocks flip. Never `docker volume rm` a registered name. | Two writers on one Postgres data dir is forbidden; a silent strand is §N6; `ensure_site_db` would start a second Postgres on the same data. | Move-to-new-cluster backup/restore (Phase 4) replacing point-in-place. |
| D-052 | 2026-08-23 | `Site.edge_owner` is the per-site Caddy record. Written by the plan or one `PATCH /api/v1/sites/{id}/` `{edge_owner}` only (must not clear `dns_zone`). Ambiguous = 80/443 whose image is not caddy/nginx/traefik, or two different edge images. Never a per-run prompt. | A per-run prompt does not persist; a PATCH that can clear `dns_zone` undoes bind. Fresh Hub sites stay `host_caddy`. | A second edge-ownership model, or PATCH growing fields that can unbind DNS. |
| D-053 | 2026-08-23 | Public bind uses matching-purpose eligible zones (`purpose=prod`; `purpose=test` only under `HUB_TEST_MODE` + name on `HUB_TEST_ZONE_SLUGS`). Site has no tier. Zero eligible → 409 and zero rows (no `site-dns-unbound`). Project create binds `primary_target` (one enrolled Target → auto; else body). Adopt refuses without a target. Plant vault shape = `API_TOKEN` / `owner_type=dns_account` / `owner_id=ref`. Plant roots = Origin-CA subdirs only. New 3.5 req ids are tier-less. `conformance-3.5` excludes t2/t3; `conformance-3` stays all-tiers Phase 3. | A `purpose=test` zone eligible outside the triple key leaks the test plane into prod bind; `site-dns-unbound:{pk}` cannot exist because an unbound public Site is illegal; a `tier:` on the new ids cannot verify on this host; an all-tiers 3.5 gate would demand live CF. | Adding `Site.tier`, inventing `HUB_TEST_CF_TOKEN`, or claiming `conformance-3` excludes live tiers. |
| D-054 | 2026-08-23 | Protective cut — §1.2. MUST = Tasks 0–10 and 12 (WebAuthn + T1 touch, D-012 SCAN-M4 + re-land, L5 playbook, never-scale, topology r1–r5, SSH rotate, backup persist/Beat/P1/list, leftover honesty, Tailscale skip-unless poll, KmsKEK T1 + DEK cache, audit hash chain, acceptance + demo). Task 11 (DNS-01 / live S3 / YubiKey / tailnet-lock writeup) is the named slip line. First slip = Hub-central DNS-01. | Pulling DNS-01, Playwright, live KMS/S3, or a 24 h Hub-down onto this wave would miss the security suite that makes T1 hardware-gated and scanner-trust honest. | Taking a slip item onto the MUST line with its spec holes answered; or bumping a MUST id off 4 before the demo. |
| D-055 | 2026-08-23 | D-012 re-land is threat-model (§1.0) + SCAN-M4 at Task 0 + re-wire of `scanner/core.py`, `fallbacks._check_secret_scan`, and wizard acceptance. Do not rewrite `scanner/declarations.py`. Invert the two “no live import” tests. Parser tests do not prove the full text. | A marker on the full-text SCAN-DECLARED ids from parked parser tests would claim operator acceptance and the five live-path attacks the moment `--phase 4` is due. Parser-only is not the threat model. | A written ruling that parser tests prove the full text; or Task 3 marking the full-text ids on live-path E2E (the intended retirement). |
| D-056 | 2026-08-23 | One schema wave: `0011_phase4.py` = `DnsZone.provider` + unique `(provider, name)` + `AuditEvent.prev_hash` + nullable `shipped_at`. Task 1 is the only `core/models.py` writer and also adds the four Python-only CheckRun kinds plus the BACKUP closed key set. No new Hub tables. django-otp-webauthn migrations are third-party. | A second Hub wave for `shipped_at` or CheckRun kinds would split the phase’s schema. New Hub tables for backup or WebAuthn devices reinvent what a path and a third-party app already give. | A later ruling that backup ciphertext needs a table, or that `shipped_at` cannot be nullable. |
| D-057 | 2026-08-23 | `edge_protection_for(zone)` is the only EdgeProtection constructor; `CloudflareEdge` lives in `providers/cloudflare.py` and loads `edge_token_ref`. Playbook degrades to notify-only when None. Attack-shaped load never scales (`scaling/attack_gate.py`). DNS client never loads `edge_token_ref`; edge client never loads `dns_token_ref`. | A second Cloudflare client, or the DNS client loading the edge token, is the SEC-B2 leak. Silent no-op when the edge ref is missing hides an under-attack site. | A provider whose edge API cannot share the Cloudflare HTTP helper; or a scaler that must act during an attack. |
| D-058 | 2026-08-23 | Tailscale device-list poll is T1 Fake + skip-unless-configured. Tailnet-lock enablement is a written evaluation, not default-on. Session↔device binding is the same shape (§J5). | A live token env invented here is the `HUB_TEST_CF_TOKEN` mistake. Default-on tailnet lock is an irreversible fleet change without a written eval. | A vault ref present on the host of record (then the skip-unless waiver retires); or a ruling that lock enablement is MUST. |
| D-059 | 2026-08-23 | AWS KMS client lives in `providers/kms.py`. `KmsKEK` takes that port and never imports boto3. Env `HUB_VAULT_KMS_KEY_ID` → `VAULT_KMS_KEY_ID` default `""`; `VAULT_KEK_BACKEND=kms` with empty key id refuses. T1 moto on the provider module. In-process DEK cache. Keyfile remains test/dev and does not defeat a stolen Hub disk. Live AWS KMS is a Joseph interrupt (~$1/mo). YubiKey rung ② may slip. | boto3 in `vault/` or `tests/` is the same “tested client is not the shipped client” split D-034 closed for DNS. Unconfigured `kms` that still constructs is a fail-open. | Joseph approving live AWS KMS; or a KMS API that cannot be faked under moto. |
| D-060 | 2026-08-23 | `conformance-4` / review-round = `--phase 4 --exclude-tier t2 --exclude-tier t3`. `conformance-3` stays all-tiers Phase 3. Do not claim it excludes live tiers. Do not add an all-tiers 4 gate. Do not extend `VALID_TIERS` with `t4`. T4 Playwright is SLIP; T1 WebAuthn protocol tests are MUST. Task 0 waives `TLS-B2-HUB-DNS01-UNPROXIED` so the gate can start. | An all-tiers 4 gate would demand live CF / KMS / S3 this host does not have. `t4` is not in the tree. Review-round staying at phase 3 would leave the security suite ungraded. | A later ruling that T4 Playwright is a review-round prerequisite; or an all-tiers 4 nightly host. |
| D-061 | 2026-08-23 | Leftover honesty: retire D-045 alias; DnsZone unique in `0011`; SEC-B4 compose parse; SEC-69 exhaust gate. `TIME_ZONE` stays UTC (SLIP operator-local). | The `alerts` alias, unpublished Redis, and exhaust greps are already named and still unproven; slipping them again is the same leftover. | A ruling that Redis crown-jewel needs an external port-scan instead of a compose parse. |
| D-062 | 2026-08-23 | T1 step-up is WebAuthn hardware touch writing `hardware_touch_at`; TOTP/recovery never write it. T3 never takes `RequireRecentTouch`. `ssh.rotate` is T1. `site.auto_mode` stays T2. Two passkeys before T1 is available. | TOTP writing `hardware_touch_at` makes a stolen TOTP session T1-capable. Rollback behind step-up chrome is the UX-F5 regression. | A ruling that TOTP may satisfy T1, or that auto-mode moves to T1. |
| D-063 | 2026-08-23 | Backup MUST includes persist + Beat + P1 `hub-db-or-backup-failure` + Sites list + test-now + restore command block + restore-drill body. Restore UI stays Phase 7. | A list API that returns ciphertext, a Restore POST, or sealing with the KEK is the wrong store. Claiming 24 h Hub-down is D-042. | Restore UI landing in Phase 7, or a ruling that backup ciphertext needs a table. |
| D-064 | 2026-08-23 | SSH rotate is dual-key overlap, probe-before-revoke, T1, run-twice zero mutating calls. Incomplete rotation is P1; stale-old-key after intended revoke is P2. | Clobbering `authorized_keys` with a single-key install during overlap locks the operator out. Overlap itself is not a Finding. | A Transport where inspect cannot be separated from mutate (D-018); or SSH-CA landing in Phase 5 replacing this playbook. |
