## Summary

## Test plan

- [ ] Named regression test if this closes a bug (`tests/test_issue_<n>_*.py`)
- [ ] `make test` green (T1). T2 only if this change needs `hub-test-target`.
- [ ] Diffs matching `conformance/paths.yaml` wait for Joseph's merge click
